{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug ubuntu-gpu-gui (Wails)",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
